# 🧩 **전체 구조 정리 — WeaponComponent, ShooterComp, Player, Mesh 관계**

## 🎮 1) PlayerCharacter (ASpaceCharacter)

역할:

- 입력 처리
    
- 이동/비행/대시
    
- 상태머신 (Idle / Aim / Charge)
    
- 카메라
    
- 무기 소켓 제공 ("WeaponSocket", "MuzzleSocket" 등)
    
- WeaponComponent + ShooterComp 보유
    

구조:

`ASpaceCharacter  │  ├── UShooterComp        ← 발사체/리로드/쿨다운/탄약  ├── UWeaponComponent    ← 무기 데이터 / FirePressed / FireReleased / 조준  └── Mesh                ← 팔/무기 Mesh가 붙는 곳`

---

## 🔫 2) ShooterComp (Fire 시스템 담당)

ShooterComp는 **무기에 상관없이 공통 발사 시스템**입니다.

역할:

- ProjectileClass 세팅
    
- 발사체 Spawn
    
- 탄약 소비
    
- Reload
    
- FireRate 쿨다운
    
- Homing/Scale/Damage 등 적용
    

**ShooterComp은 오로지 “발사 기능”만 담당**  
무기 정보/데이터(데미지, 탄약량)는 WeaponComponent가 제공할 수 있음.

---

## 🔥 3) WeaponComponent (무기 로직 담당)

WeaponComponent의 역할:

- 어떤 무기인지 선택 (WeaponType or DataAsset)
    
- 사격 입력 처리
    
    - HandleFirePressed()
        
    - HandleFireReleased()
        
- 차지(Charge) 시작/종료
    
- 탭 발사/차지 발사 구분
    
- GetAimPoint()로 조준점 계산
    
- GetMuzzleLoc()으로 발사 지점 계산 (Player Mesh 기반)
    

### WeaponComponent를 Player에 붙여놓는 이유

- 입력을 직접 받아 처리할 수 있음
    
- PlayerMesh를 통해 조준/발사 방향 계산 가능
    
- 무기 교체 시 WeaponComponent의 데이터만 바꾸면 됨
    
- 무기 Mesh 교체는 PlayerCharacter의 Mesh에 붙이면 됨
    

WeaponComponent는 **논리적인 “Weapon Controller”**입니다.

---

## 💽 4) WeaponData (FWeaponData + DataTable or DataAsset)

각 무기가 가지는 수치들을 담습니다.

예:

`Damage ReloadTime FullAmmo MaxAmmo Spread Recoil FireRate ProjectileType bIsHitScan bIsChargeWeapon`

WeaponComponent는 InitializeWeapon()에서 이 데이터를 읽음.

---

## 🧱 5) 무기 Mesh는 어디에 있나?

WeaponComponent가 아니라  
**PlayerCharacter의 Mesh에 붙습니다.**

### 예)

플레이어 Mesh의 손 뼈(Hand_R)에:

`USkeletalMeshComponent* WeaponMesh`

WeaponComponent는 Mesh를 갖지 않음.

---

# 🧱 전체 도식화

`ASpaceCharacter  ├── Mesh (SkeletalMesh)  │    └── WeaponMesh (무기 모델) ← Socket("Weapon_R")에 연결  │          └── Socket("Muzzle") ← 발사 위치  │  ├── UWeaponComponent (무기 로직)  │      ├── WeaponData (FWeaponData)  │      ├── HandleFirePressed()    ← 입력  │      ├── HandleFireReleased()   ← 입력  │      ├── GetAimDirection()      ← 카메라 기반  │      ├── GetMuzzleLoc()         ← PlayerMesh 의 WeaponSocket 기준  │      └── PerformFire()          ← ShooterComp로 발사  │  └── UShooterComp (발사 기능)         ├── TryFire()         ├── Fire()         ├── Reload()         ├── ProjectileClass         ├── Homing / Arc / Damage         └── Cooldown + Ammo`

---

# 📌 무기 교체 시 동작 구조

무기 변경하려면:

`WeaponComponent->SetWeaponRowName(NewRowName); WeaponComponent->InitializeWeapon(Player, ShooterComp); Player->SetWeaponMesh(NewMesh);`

이 3줄이면 끝.

WeaponComp은 수치/로직  
ShooterComp은 발사  
WeaponMesh는 Player의 Mesh가 관리

**역할이 완벽히 분리됨.**


무기 종류
크게 나눌 수 있는 것
무게, EN 부하, 탄창, 연사력, 공격력, 장전속도, 탄속
HandGun A
HandGun B
Rifle A
Rifle B
Launcher A
Blast
ShotGun A
ShotGun B

장비
경량
중량